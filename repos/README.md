# repos

This folder stores cloned reference repositories declared in `repos.yaml`.

`python -m scix install-repos` and `python -m scix up` clone missing repos
here.

The contents of this folder are ignored by Git except for this README and
`.gitkeep`.
